// frontend/src/assets/theme-dark/components/dialog/dialogContent.js

const dialogContent = {
  styleOverrides: {
    root: {
      padding: "16px 24px",
      // Ensure background and text colors are not overridden here
      // They should inherit from MuiDialog.paper or be set by MuiDialogContentText
    },
  },
};

export default dialogContent;
